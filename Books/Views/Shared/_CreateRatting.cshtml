@model string;

@if (User.Identity.IsAuthenticated)
{
    <div style="z-index: 1; position: fixed; right: 25px; bottom: 75px;" class="text-white">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Đánh giá sách
        </a>
    </div>
}
